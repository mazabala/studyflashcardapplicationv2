// lib/core/themes/colors.dart

import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primaryColor = Color(0xFF00796B);  // Dark Teal - Bold and refined
  static const Color secondaryColor = Color(0xFF388E3C);  // Rich Green - Stronger contrast
  static const Color accentColor = Color(0xFF2C6B3A);  // Deep Forest Green - More grounded and professional

  // Background Colors
  static const Color backgroundColor = Color(0xFF2F4F4F);  // Dark Slate Blue - Subtle and calming
  static const Color cardBackgroundColor = Color(0xFFE0E0E0);  // Light Grey - Minimal, subtle contrast

  // Text Colors
  static const Color textPrimaryColor = Color(0xFF212121);  // Darker Charcoal - Strong, clear contrast for text
  static const Color textSecondaryColor = Color.fromARGB(255, 192, 192, 192);  // Muted Gray - Softer but still readable


  // Error Color
  static const Color errorColor = Color(0xFFB71C1C);  // Red for errors
}

